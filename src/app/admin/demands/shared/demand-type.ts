export enum DemandType {
  warm_lunch = 'warm_lunch',
  grocery = 'grocery',
  medicine = 'medicine',
  invoices = 'invoices',
  transport = 'transport',
}

export const demandTypes = Object.values(DemandType);
