version: "3.7"

services:
  covid19md-dev:
    container_name: covid19md-dev
    build:
      context: .
      dockerfile: Dockerfile-dev
    volumes:
      - ".:/app"
      - "/app/node_modules"
    ports:
      - "4200:80"
    environment:
      - CHOKIDAR_USEPOLLING=true
